# Nombre de la aplicación
spring.application.name=MotvirsWeb

# Configuración del modelo
motvirs.model.path=modelo_satisfaccion_j48.model

# Configuración de MongoDB (URI corregida)
spring.data.mongodb.uri=mongodb+srv://mateo:123@motvirs.uaqijwe.mongodb.net/motvirsweb?retryWrites=true&w=majority&appName=Motvirs

# Configuración de puerto para Azure
server.port=${HTTP_PLATFORM_PORT:8080}
server.servlet.context-path=/

# Configuración de logging (reducir para producción)
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO
logging.level.com.example.MotvirsWeb=DEBUG

# Configuración de correo
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vanegasdr276@gmail.com
spring.mail.password=ajoi zpmu kovt apix
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Configuración para Azure
spring.main.allow-bean-definition-overriding=true
spring.main.lazy-initialization=false

# URL base para Azure (IMPORTANTE: cambiar localhost por la URL de tu app)
app.base-url=${APP_BASE_URL:http://localhost:8080}/public/home/pasajero

# Configuración de Tomcat para Azure
server.tomcat.connection-timeout=60000
server.tomcat.max-connections=10000
